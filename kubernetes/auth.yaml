apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth
  template:
    metadata:
      labels:
        app: auth
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "my-app-role"
        vault.hashicorp.com/agent-inject-secret-env-vars: "secret/data/my-app/env"
        vault.hashicorp.com/agent-inject-template-env-vars: |
          {{- with secret "secret/data/my-app/env" -}}
          {{- range $k, $v := .Data.data }}
          export {{ $k }}="{{ $v }}"
          {{- end }}
          {{- end -}}
    spec:
      serviceAccountName: my-app-sa
      initContainers:
        - name: wait-for-postgres
          image: postgres:16
          command: ["sh", "-c", "until pg_isready -h postgres-auth -p 5432; do sleep 2; done"]
      containers:
        - name: auth
          image: auth:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 5001
          
          # Injects non-sensitive configs from ConfigMap
          envFrom:
            - configMapRef:
                name: app-config
          
          # Loads secrets from Vault, then executes the application
          command: ["/bin/sh", "-c"]
          args:
            - |
              . /vault/secrets/env-vars
              
              exec /docker-entrypoint.sh
---
apiVersion: v1
kind: Service
metadata:
  name: auth
spec:
  selector:
    app: auth
  ports:
    - port: 5001
      targetPort: 5001