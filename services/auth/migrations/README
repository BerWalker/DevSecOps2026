Auth service database migrations.

Run from project root:

```bash
export FLASK_APP="services.auth.app"
export AUTH_DATABASE_URL="postgresql+psycopg://auth:auth@localhost:5432/auth_db"
flask db upgrade
```
